import React from 'react';
import "./index.css"

class SecurityWindow extends React.Component {
    construct() {
    }
    readonly render: () => React.ReactNode = (): React.ReactNode => {
        return <div>Security</div>;
    }
};

export default SecurityWindow;